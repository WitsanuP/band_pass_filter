#include <iostream>
#include <fstream>
#include <iomanip>
#include <string>

int main() {
    // ????????????? Input ??? Output
    std::string inputFilename = "input.txt";
    std::string outputFilename = "output.txt";

    // ??????????????????
    std::ifstream inputFile(inputFilename);
    if (!inputFile.is_open()) {
        std::cerr << "Error: ????????????????? " << inputFilename << " ???\n";
        return 1;
    }

    // ???????????????????
    std::ofstream outputFile(outputFilename);
    if (!outputFile.is_open()) {
        std::cerr << "Error: ?????????????????? " << outputFilename << " ???\n";
        return 1;
    }

    // ????????? 2^13 = 8192
    //const double MULTIPLIER = 8192.0;
    const double MULTIPLIER = 4096.0;
    double number;

    // ??????? Format ??????? Output (??????????????????? 6 ???????)
    outputFile << std::fixed << std::setprecision(1);

    // ?????????????? input ???????, ??????? 8192, ?????????????? output
    while (inputFile >> number) {
        outputFile << (number * MULTIPLIER) << "\n";
    }

    // ??????????????????????
    inputFile.close();
    outputFile.close();

    std::cout << "??????????????! ????????????????? " << outputFilename << " ?????????????\n";

    return 0;
}
