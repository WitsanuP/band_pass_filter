 
#include <iostream>
#include <cmath>
#include <string>

using namespace std;

// ???????????????????????????? Fixed-point Binary (2's Complement)
string to_fixed_point_binary(double value, int frac_bits) {
    int total_bits = 1 + 1 + frac_bits; // 1 (sign) + 1 (int) + x (frac)
    
    // ????????????????????????????? Fixed-point (??????? 2^frac ???????????????????????????)
    long long int_val = round(value * pow(2, frac_bits));
    
    // ????? String ?????????????
    string binary_str = "";
    
    // ?????? Bit ???????????????????????? (MSB -> LSB)
    for (int i = total_bits - 1; i >= 0; --i) {
        binary_str += ((int_val >> i) & 1) ? '1' : '0';
    }
    
    return binary_str;
}

int main() {
    double dec;
    int frac;
    
    while(1){
    // ?????? Input ????????????
    cout << "Enter decimal value: ";
    cin >> dec;
    cout << "Enter fractional bits (frac): ";
    cin >> frac;
    
    int total_bits = 1 + 1 + frac; // ????????????????????
    string bin_str = to_fixed_point_binary(dec, frac);
    
    cout << "\n--- Output ---\n";
    
    // ????????????????????? 1
    cout << "//<sign=1, int=1, frac=" << frac << ">  dec = " << dec << "\n";
    
    // ????????????????????? 2 (???? 6'sb000000;)
    cout << total_bits << "'sb" << bin_str << ";\n";
}
    
    return 0;
}
